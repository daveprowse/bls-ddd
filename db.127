$ORIGIN example.com.
$TTL	604800
@	IN	SOA	debserver.example.com. hostmaster.example.com. (
			      3		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 	; Negative Cache TTL
			 )

; name servers - NS records
example.com.			IN	NS	debserver.example.com.

; PTR Records
51.0.21.172.in-addr.arpa. 	IN	PTR	debserver.example.com.
